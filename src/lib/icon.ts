// Every icon on the site takes these props, so the icons match the
// square system: 1.5px stroke, square caps, mitred joins. Sizes are 16, 20, 24.
export const ICON = {
  strokeWidth: 1.5,
  strokeLinecap: "square",
  strokeLinejoin: "miter",
  "aria-hidden": true,
} as const;
